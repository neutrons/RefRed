To build the HTML documentation, type in a terminal:

$> cd /path/to/RefRed/dev-docs/
$> make html

The generated entry-point HTML page becomes _build/html/index.html


For detailed information on building the documentation in other formats, type:
$> make help